UI-1	UI	브라우저	Viewport DOM 감지	상	IntersectionObserver로 화면 내 텍스트 스캔
UI-2			스크롤 200ms 정지 시 연산 트리거	상	"무한 스크롤(SNS, 댓글 등)를 방지하기 위하여
childList, subtree 변화를 감지하여 신규 생성된 DOM에만 observer 추가"
UI-3			CSS Blur 마스킹 주입	상	AI 검출 True 시 해당 컨텐츠 filter: blur & 클릭 시 해제
UI-4			XAI 사유 제공 툴팁	중	"AI 검출이 된 컨텐츠에 추가 정보 제공 
ex) AI 확신도: 88% (어휘 다양성 32% - 상투어 반복, 문장 변동성 낮음)
"
UI-5		사용자 설정	감도 임계값 슬라이더 조절	중	"사용자 설정 화면에서 필터링 감도를 조절 
로컬 저장소에 설정 값 저장
"
UI-6			도메인 예외 처리	중	"사용자 설정 화면에서 AI 필터링을 원히지 않는 브라우저의 주소를 설정
로컬 저장소에 설정 값 저장
"
UI-7			통계 대시보드	하	"Chart.js 기반 일별/주별  탐지 통계 차트 제공
stats 테이블에서 오늘 마스킹한 총 텍스트 수, 평균 AI 확신도 조회"
SV-1	서버	API 엔드 포인트 생성	DETECT_TEXT	상	텍스트 분석 연산 요청
SV-2			GET_CACHE	상	로컬 DB 캐시 조회
SV-3			REPORT_FFEDBACK	하	오탐/미탐 피드백 저장
SV-4			GET_RULES	상	"현재 적용된 로컬 규칙 조회
필터링 감도 / 예외 도메인 
"
SV-5			GET_STARTS	중	대시보드 통계 데이터 조회
SV-6		서비스 관리	피드백/오탐 수집 핸들러	하	사용자의 오탐 피드백 저장
SV-7			데이터 모델 및 ORM 정의	상	분석 타겟 / 반환 / 로컬 규칙 설정 / 통계
SV-8		배포	로컬 실행 환경 패키징	하	DOM 계산과 무관한 모든 연산을 detector.worker.ts로 묶어 백그라운드 쓰레드에서 격리 실행
SV-9			로컬 서버 실행 & 테스트	하	Vitest를 사용해 100개의 AI/Human 샘플 데이터를 기반으로 알고리즘 정확도 및 연산속도 유닛 테스트 수행
MD-1	모델	알고리즘 기반 탐지 엔진	AI/Human 원문 데이터셋 기준 검증	상	원문 필요 시 정리, HC3(Human ChatGPT Comparison Corpus) 한국어/영어 데이터셋 활용
MD-2			문장 구조 단조로움 계산	상	"문장 길이의 표준편차 계산
문장 길이가 일정함에 따라 AI 확률 증가
문장부호 규칙성(일정한 사용), AI의 상투적인 표현 사전 구축, 문장 연결 및 종결 표현 반복률, 문장 구조의 유사성 계산"
MD-3			어휘 다양성(TTR) & 엔트로피 수치 연산	상	반복적인 어휘 및 특정 영역에서 쏠릴수록 AI 확률 증가
MD-4			AI 상투어/N-gram 패턴 검출	상	"""종합적으로 볼 때"", ""결론적으로 말해서"" 등 AI 상투어를 
N-gram 딕셔너리 기반 정규식을 통해 매칭

"
MD-5			텍스트 언어학적 지표 -> Feature Vector 변환	상	"문장 구조 단조로움 계산 & TTR & N-gram를 기반으로 
계산된 수치들을 5차원 벡터로 졍규화 ([0, 1] 범위)
"
MD-6		로컬 임베팅 & 벡터 연산	로컬 벡터 간 거리 연산	상	코사인 유사도 검사를 통해 원문에서 추출한 벡터와 로컬 DB에 저장된 전형적 AI 백터 세트간의 거리를 계산
MD-7			지표별 가중치/파라미터 튜닝	상	최종 AI 점수 산출 복합식을 통한 확률 계산
DB-1	데이터베이스	환경세팅	로컬 DB 환경 구축	상	로컬 DB 설치 및 연동 - DB 명세서 참고해 테이블 생성 (Boolean으로 구분 가능)
DB-2			캐시 & 벡터 인덱스 테이블 스키마 생성	상	캐싱, 로컬 벡터 스토어, 사용자 오탐 제보에 필요한 필요한 테이블 생성
DB-3		캐싱	SHA-256 PK 기반 조회	중	"crpto.subtle.digest('SHA-256', encoder.encode(text))로 빠른 해시 생성 후 
db.text_cache,get(hash) 조회를 실행하여 캐시 히트 판정
"
DB-4			로컬 용량 관리를 위한 LRU Purge	상	"최대 10,000건의 캐시 데이터 저장 
초과 시 created_at 기준 가장 오래된 상위 2,000건을 일괄 삭제
"
DB-5		로컬 벡터 스토어 & 룰 DB	규칙 저장소 생성	중	user_rules Key-Value 저장소를 만들어 사용자가 추가한 커스텀 차단 단어 및 설정 값 관리
DB-6			로컬 Feature Vector 유사도 색인	상	"메모리 내에 feature_vectors 데이터 테이블을 캐싱해두고,
신규 텍스트의 feature_vector와 코사인 거리 연산을 백그라운드에서 실행"