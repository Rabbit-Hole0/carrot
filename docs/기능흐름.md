[ 사용자가 웹페이지 접속 / 스크롤 ]
              │
              ▼
 [ DOM Observer (Content Script) ]
  • IntersectionObserver로 화면 내 뷰포트 텍스트 감지
  • 200ms Debounce 정지 감지
  • DOM 안전 검사 (Virtual DOM 및 React/Vue 래퍼 유효성 확인)
              │
              ▼
 [ Web Crypto API (Local) ]
  • 텍스트 원문의 SHA-256 해시 생성 (예: "a8f5c...")
              │
              ▼
 [ Cache Lookup (IndexedDB: text_cache) ]
              │
      ┌───────┴────────────────────────────────┐
      ▼                                        ▼
 [ Cache Hit ]                           [ Cache Miss ]
  • 로컬 DB에서 $O(1)$ 속도로 판정값 로드    • 알고리즘 엔진(Web Worker)으로 텍스트 전달
      │                                        │
      │                                        ▼
      │                          [ Pure JS Engine 연산 ]
      │                           1. Burstiness (문장 길이 표준편차 계산)
      │                           2. TTR & Entropy (어휘 다양성)
      │                           3. N-gram 사전 매칭 (`ngram_freq.bin`)
      │                                        │
      │                                        ▼
      │                          [ Decision Tree Evaluation ]
      │                           • 계산된 수치를 경량 트리 모델에 투입
      │                           • $1\text{ms}$ 만에 최종 $P_{AI}$ 스코어 산출
      │                                        │
      │                                        ▼
      │                          [ DB Store & Purge ]
      │                           • 산출 결과를 `text_cache`에 저장
      │                           • 10,000건 초과 시 LRU Purge 자동 실행
      │                                        │
      └─────────────────────────┬──────────────┘
                                │
                                ▼
               [ $P_{AI} > \text{Threshold}$ ? ]
               (설정값 `chrome.storage.local`과 비교)
                                │
                      ┌─────────┴─────────┐
                     YES                 NO
                      │                   │
                      ▼                   ▼
     [ Shadow Mask Renderer ]         [ Normal Text ]
      • 비파괴적 DOM 래핑 처리        마스킹 없이 통과
      • CSS `filter: blur(5px)` 주입
      • Shadow DOM XAI 툴팁 바인딩
      • 클릭 시 마스킹 즉시 해제
