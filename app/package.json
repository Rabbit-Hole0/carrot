{
  "name": "rabbit-carrot",
  "description": "manifest.json description",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "dev:firefox": "wxt -b firefox",
    "build": "wxt build",
    "postbuild": "node scripts/sanitize-extension-output.mjs",
    "build:firefox": "wxt build -b firefox",
    "postbuild:firefox": "node scripts/sanitize-extension-output.mjs",
    "zip": "wxt zip",
    "zip:firefox": "wxt zip -b firefox",
    "compile": "tsc --noEmit",
    "postinstall": "wxt prepare"
  },
  "devDependencies": {
    "@types/webextension-polyfill": "^0.12.5",
    "typescript": "^5.9.3",
    "vite": "^8.1.5",
    "web-ext": "^10.5.0",
    "wxt": "^0.20.27"
  },
  "dependencies": {
    "dexie": "^4.4.4",
    "webextension-polyfill": "^0.12.0"
  }
}
